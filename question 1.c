#include<stdio.h>
#include<conio.h>
int main(){
  int num;
    printf("enter a num");
    scanf("%d",&num);
      if(num<0)
        printf("%d is a negative number",num);
      else
        printf("%d is a positive number",num);

 return 0;
}
